import { HeaderBaner } from './HeaderBaner/HeaderBaner'
import { HeaderTop } from './HeaderTop/HeaderTop'

export const Header = () => {
    return (
        <header>
            <HeaderTop />
            <HeaderBaner />
        </header>
    )
}
